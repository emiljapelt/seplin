
struct tuple<T,U>(fst: T, snd: U);

entry main() {
    
    o ::= 1;
    s :stable:= 2;
    c :const:= 3;

    t :tuple:= {o,2};


    t := {o, 5};

    tuple_swap({1,2});
}

internal tuple_swap<T,U>(t: tuple<T,U>) {
    tmp ::= t.fst;
    t.fst := t.snd;
    t.snd := tmp;
}