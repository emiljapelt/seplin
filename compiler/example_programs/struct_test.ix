funny : locked int := 69;

struct tuple [fst: int, snd: int];
struct person [age: int, favoriteNumbers: tuple];

external str() {
    x: var := new tuple[4,5];
    print x[fst];
    print x[snd];
}

external arr() {
    x: var := new int[3];
    a: int := 4;
    b: int := 5;
    x[0] := 2;
    x[1] := 3;
    print x[0];
    print x[1];
    print |x|;
}

internal init_array(i: int, b: bool, arr: int[], t: tuple) {
    i := 2;
    b := true;
    arr[0] := i;
    t[fst] := i;
}

external pers() {
    emil: var := new person[23, new tuple[8, 16]];
    
    print_person(emil);
    print emil[favoriteNumbers][fst];
}

internal print_person(p: locked person) {
    print p[age];
    print p[favoriteNumbers][fst];
    print p[favoriteNumbers][snd];
}