

reference ./f3.ix as list;

meme1 : int := 1;
meme2 : int := meme1 + 2;

entry main1() {
    
    nums :list<int>:= {1, {2, {3, null}}};
    sum: int;

    list#sum(nums, sum);
    print sum, '\n';

    list#prepend(6, nums);
    list#sum(nums, sum);
    print sum, '\n';

}